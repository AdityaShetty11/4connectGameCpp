# Connect Four

A console-based Connect Four game written in modern C++.

Two players take turns dropping pieces into a 7-column board until one connects four.

Build with `make` and run with `make run`.
